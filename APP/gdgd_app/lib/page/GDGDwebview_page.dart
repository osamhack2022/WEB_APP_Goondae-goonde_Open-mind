import 'package:flutter/material.dart';

class GDGDWebView extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
    backgroundColor: Colors.black,
    body: Center(
      child: Text(
        'GDGD 웹뷰',
        style: TextStyle(fontSize: 30, color: Colors.white),
      ),
    ),
  );
}